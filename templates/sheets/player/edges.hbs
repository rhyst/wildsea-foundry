<div id="edges" class="flex0">
  <h2 class="corners dark">{{localize "wildsea.edges"}}</h2>
  <div class="bordered">
    {{#each edgesList as |row|}}
      <div class="flexrow spaced wide">
        {{#each row as |edge|}}
          {{#if edge}}
            {{> "systems/the-wildsea/templates/sheets/player/list_track.hbs"
              name=(localize (concat "wildsea." edge))
              value=(byKey @root.system.edges edge)
              max=1
              id=edge
              type="edge"}}
          {{else}}
            <div class=""></div>
          {{/if}}
        {{/each}}
      </div>
    {{/each}}
  </div>
</div>
