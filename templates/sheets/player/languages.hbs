<div id="languages" class="flex0">
  <h2 class="wildsea-cut-corners wildsea-bg-dark">{{localize "wildsea.languages"}}</h2>
  <div class="wildsea-section-body">

    {{#each languagesList as |row|}}
      <div class="flexrow wildsea-gapped wide">
        {{#each row as |language|}}
          {{#if language}}
            {{> "systems/the-wildsea/templates/sheets/player/list_track.hbs"
              name=(localize (concat "wildsea." language))
              value=(byKey @root.system.languages language)
              max=@root.config.languageMax
              id=language
              type="language"}}
          {{else}}
            <div class=""></div>
          {{/if}}
        {{/each}}
      </div>
    {{/each}}

  </div>
</div>
